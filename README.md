Notes
---

From the project root, install the package in editable mode:

```bash
python3 -m pip install --editable .
```

This makes the `note` command available from your shell while pointing at the current source tree.

